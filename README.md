# envie-kotlin

Kotlin Multiplatform port of the Rust `envie` crate, with `.env` loading and
process-environment helpers across the Kotlinmania target matrix.

Publication coordinates are configured as:

```kotlin
implementation("io.github.kotlinmania:envie-kotlin:0.1.0")
```

As of this repository state, the artifact has not been released to Maven
Central yet.
